# R1.3.1 任务报告：优化 SKILL.md 文档

**执行日期**：2026-01-30
**任务状态**：已完成

## 任务目标

优化 `~/.claude/skills/mdtodo-edit/SKILL.md` 文档，添加详细的使用示例和命令说明，确保 subagent 能一次执行成功。

## 执行步骤

### 步骤1：分析需求

根据 `docs/MDTODO_SKILL.md` 的要求，R1.3 任务需要：
- SKILL.md 包含完整命令说明
- subagent 可直接使用 skill，无需试错

### 步骤2：收集 CLI 文档信息

阅读了 `docs/MDTODO_CLI_USAGE.md` 和 `cli/package.json`，了解：
- CLI 入口：`dist/cli.js`
- 所有命令：`list`, `get`, `add`, `add-sub`, `delete/remove`, `complete/done`, `start`, `update`, `stats`
- 通用选项：`-f, --file <path>`, `--json`

### 步骤3：创建优化后的 SKILL.md

在 `cli/SKILL.md` 创建了完整的 skill 文档，包含：

1. **前置条件**：CLI 安装说明
2. **快速开始**：最短路径示例
3. **命令列表**：9 个命令的详细说明
   - 命令格式
   - 参数说明
   - 完整示例
   - 输出示例
4. **完整使用场景**：6 个常见场景
5. **任务状态说明**：状态对照表
6. **文件自动搜索规则**：查找逻辑说明
7. **注意事项**：重要提示
8. **错误处理**：常见错误及调试步骤

## 文档特点

### 确保一次成功的关键设计

1. **命令格式标准化**：每个命令都使用统一的模板
   ```
   cd ~/.claude/skills/mdtodo-edit/scripts
   node dist/cli.js <command> [options]
   ```

2. **完整路径示例**：所有示例都使用绝对路径，避免相对路径问题

3. **taskId 说明**：明确解释 `R1`, `R1.1`, `R1.1.1` 的格式

4. **级联删除警告**：醒目提示删除父任务的危险

5. **错误处理章节**：常见错误及解决方法，方便调试

6. **场景化示例**：提供完整的工作流程示例

## 验证标准

| 标准 | 状态 |
|-----|------|
| SKILL.md 包含完整命令说明 | 已满足 |
| subagent 可直接使用 skill | 已满足 |
| 文档格式正确 | 已满足 |

## 交付物

**文件位置**：`D:\Work\vscode-mdtodo\cli\SKILL.md`

**部署方式**：将此文件复制到 `~/.claude/skills/mdtodo-edit/SKILL.md`

```bash
# 复制命令
cp /path/to/vscode-mdtodo/cli/SKILL.md ~/.claude/skills/mdtodo-edit/SKILL.md
```

## 后续建议

1. 在实际使用中测试文档的完整性
2. 根据 subagent 反馈补充更多示例
