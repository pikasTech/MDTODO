# R1.3.2 任务报告：审查查询命令（list、get）实现

## 任务概述

审查 R1.3.1 的执行报告，验证 list 命令是否可列出所有任务、get 命令是否可按 ID 查询任务、是否支持 JSON 输出格式。

## 审查时间

2026-01-30

## 审查结果

### 1. list 命令验证

**验证命令**：
```bash
node dist/cli.js --file "../docs/20260120_MDTODO.md" list
```

**验证结果**：通过

**实际输出**：
```
[x] R1 深入调研 VSCODE 插件开发相关知识，包括联网调研，形成调研报告。
  [x] R3.1 #  第1阶段详细执行计划...
...
统计: 共 139 个任务，已完成 139，进行中 0，待处理 0
```

**功能验证**：
- [x] 正确显示任务树状结构
- [x] 使用缩进表示层级关系
- [x] 状态标记正确（[x] 已完成、[-] 进行中、[ ] 待处理）
- [x] 统计信息准确

### 2. get 命令验证

**验证命令**：
```bash
node dist/cli.js --file "../docs/20260120_MDTODO.md" get R1
```

**验证结果**：通过

**实际输出**：
```
任务 ID: R1
标题: 深入调研 VSCODE 插件开发相关知识...
状态: 已完成
行号: 10
链接统计: 1/1
```

**功能验证**：
- [x] 正确显示任务 ID
- [x] 正确显示任务标题
- [x] 正确显示任务状态
- [x] 正确显示行号
- [x] 正确显示链接统计

### 3. JSON 输出格式验证

**验证命令**：
```bash
# list JSON 输出
node dist/cli.js --file "../docs/20260120_MDTODO.md" list --json

# get JSON 输出
node dist/cli.js --file "../docs/20260120_MDTODO.md" get R3.2 --json
```

**验证结果**：通过

**实际输出**：
```json
{
  "id": "R3.2",
  "title": "第2阶段详细执行计划...",
  "completed": true,
  "processing": false,
  "children": [],
  "lineNumber": 29,
  "linkCount": 1,
  "linkExists": 1
}
```

**功能验证**：
- [x] `--json` 全局选项对所有命令生效
- [x] JSON 输出结构完整
- [x] 包含所有必要字段（id, title, completed, processing, children, lineNumber, linkCount, linkExists）

### 4. 错误处理验证

**验证命令**：
```bash
node dist/cli.js --file "../docs/20260120_MDTODO.md" get R999
```

**验证结果**：通过

**实际输出**：
```
错误: 未找到任务: R999
```

**功能验证**：
- [x] 查询不存在的任务返回错误信息
- [x] 正确退出码（exit code 1）

## 审查清单

| 审查标准 | 状态 | 说明 |
|---------|------|------|
| list 命令可列出所有任务 | **通过** | 显示任务树状结构和统计信息 |
| get 命令可按 ID 查询任务 | **通过** | 显示任务详细信息 |
| 支持 JSON 输出格式 | **通过** | `--json` 全局选项正常工作 |

## 代码审查

### CLI 入口文件

**文件**: `D:\Work\vscode-mdtodo\cli\src\cli.ts`

#### list 命令实现（第 112-129 行）
```typescript
program
  .command('list')
  .description('列出所有任务')
  .action(async (options: CLIOptions, cmd: Command) => {
    const cli = new MDTODOCLI();
    const filePath = cli.resolveFilePath(cmd.parent?.opts().file);
    const { manager } = cli.loadFile(filePath);
    const tasks = manager.getTasks();

    if (cmd.parent?.opts().json) {
      console.log(JSON.stringify(tasks, null, 2));
      return;
    }

    cli.printTasks(tasks, 0);
    const stats = manager.getStats();
    console.log(`\n统计: 共 ${stats.total} 个任务，已完成 ${stats.completed}，进行中 ${stats.inProgress}，待处理 ${stats.pending}`);
  });
```

#### get 命令实现（第 132-152 行）
```typescript
program
  .command('get <id>')
  .description('获取指定任务详情')
  .action(async (id: string, options: CLIOptions, cmd: Command) => {
    const cli = new MDTODOCLI();
    const filePath = cli.resolveFilePath(cmd.parent?.opts().file);
    const { manager } = cli.loadFile(filePath);
    const task = manager.getTask(id);

    if (!task) {
      console.error(`错误: 未找到任务: ${id}`);
      process.exit(1);
    }

    if (cmd.parent?.opts().json) {
      console.log(JSON.stringify(task, null, 2));
      return;
    }

    cli.printTaskDetails(task);
  });
```

## 结论

R1.3.1 任务执行结果已通过审查：

1. **list 命令** - 功能完整，实现正确
2. **get 命令** - 功能完整，包含错误处理
3. **JSON 输出** - 支持 `--json` 全局选项

所有验证标准均已满足，R1.3 任务可以标记为完成。

## 后续任务

R1.3.2 完成后，可进行以下任务：

- R1.4: 实现添加命令（add、add-sub）
- R1.5: 实现删除命令（delete）
- R1.6: 实现更新命令（update、complete、start）
- R1.7: 复制测试数据
- R1.8: 编写单元测试
