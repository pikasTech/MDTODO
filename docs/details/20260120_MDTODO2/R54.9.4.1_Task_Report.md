# R54.9.4.1 任务报告

## 任务概述

**任务ID**: R54.9.4.1
**任务目标**: 修改 claudeService.ts，添加 opencode 执行方法，根据 executionMode 选择执行的 CLI 工具
**任务状态**: [completed]
**完成时间**: 2026-02-02

## 需求分析

根据 R54.9 的总体需求，需要支持执行按钮选择使用 Claude 或 OpenCode 执行任务。本任务的具体要求：

1. 在 claudeService.ts 中添加 `executeTaskWithOpenCode` 方法
2. 修改 `executeTask` 方法，根据 `executionMode` 配置选择执行 claude 或 opencode
3. 从 settingsService 获取 `executionMode` 配置

## 实现方案

### 1. 导入 SettingsService

在 claudeService.ts 中导入 SettingsService：

```typescript
import { SettingsService } from './settingsService';
```

### 2. 添加 SettingsService 实例

在 ClaudeService 类中添加 settingsService 私有属性：

```typescript
export class ClaudeService {
  private outputChannel: vscode.OutputChannel;
  private currentProcess: ChildProcess | undefined;
  private settingsService: SettingsService;

  constructor() {
    this.outputChannel = vscode.window.createOutputChannel('MDTODO Claude');
    this.settingsService = new SettingsService();
  }
}
```

### 3. 修改 executeTask 方法

修改 `executeTask` 方法，从 settingsService 获取 executionMode，并根据配置选择执行方式：

```typescript
async executeTask(todoFilePath: string, taskId: string): Promise<ExecutionResult> {
  // 获取工作区路径
  const workspacePath = getWorkspaceFolderPath();
  if (!workspacePath) {
    return {
      success: false,
      output: '',
      error: '未找到工作区路径'
    };
  }

  // 从 settingsService 获取 executionMode 配置
  const executionMode = await this.settingsService.getExecutionMode(workspacePath);

  // 根据 executionMode 选择执行方式
  if (executionMode === 'opencode') {
    return this.executeTaskWithOpenCode(todoFilePath, taskId);
  } else {
    return this.executeTaskWithClaude(todoFilePath, taskId);
  }
}
```

### 4. 添加 executeTaskWithClaude 方法

将原有的 executeTask 逻辑重构为 `executeTaskWithClaude` 私有方法：

```typescript
/**
 * 【R54.9.4.1】使用 Claude 执行任务
 */
private async executeTaskWithClaude(todoFilePath: string, taskId: string): Promise<ExecutionResult> {
  const os = platform();

  this.outputChannel.show();
  this.outputChannel.appendLine(`开始执行任务: ${taskId}`);
  this.outputChannel.appendLine(`文件: ${todoFilePath}`);
  this.outputChannel.appendLine('模式: Claude Code');
  this.outputChannel.appendLine('---');

  // ... 原有执行逻辑 ...

  return {
    success: true,
    output: `任务 ${taskId} 已提交执行（Claude Code），请查看新打开的终端窗口`
  };
}
```

### 5. 添加 executeTaskWithOpenCode 方法

新增 `executeTaskWithOpenCode` 方法，使用 opencode 命令执行任务：

```typescript
/**
 * 【R54.9.4.1】使用 OpenCode 执行任务
 */
private async executeTaskWithOpenCode(todoFilePath: string, taskId: string): Promise<ExecutionResult> {
  const os = platform();

  this.outputChannel.show();
  this.outputChannel.appendLine(`开始执行任务: ${taskId}`);
  this.outputChannel.appendLine(`文件: ${todoFilePath}`);
  this.outputChannel.appendLine('模式: OpenCode');
  this.outputChannel.appendLine('---');

  // ... 使用 opencode 命令执行 ...

  return {
    success: true,
    output: `任务 ${taskId} 已提交执行（OpenCode），请查看新打开的终端窗口`
  };
}
```

## 修改文件

| 文件路径 | 修改类型 |
|---------|---------|
| `src/services/claudeService.ts` | 修改 |

## 修改内容摘要

1. **导入依赖**: 添加 `SettingsService` 导入
2. **类属性**: 添加 `settingsService: SettingsService` 私有属性
3. **构造函数**: 初始化 `settingsService` 实例
4. **executeTask 方法**: 重构为路由方法，根据 executionMode 选择执行器
5. **executeTaskWithClaude 方法**: 新增私有方法，原有 Claude 执行逻辑
6. **executeTaskWithOpenCode 方法**: 新增私有方法，OpenCode 执行逻辑

## 配置获取

通过 `SettingsService.getExecutionMode(workspacePath)` 获取执行模式配置：

- `'claude'`: 使用 Claude Code 执行任务（默认）
- `'opencode'`: 使用 OpenCode 执行任务

配置存储在 `.vscode/mdtodo/settings.json` 文件中。

## 日志记录增强

在日志记录中添加了 `executor` 字段，用于区分执行器：

```typescript
await logTaskEvent(logsDir, todoFilePath, taskId, 'taskExecute', {
  command: fullCommand,
  mode: 'new_terminal',
  platform: os,
  executor: 'claude'  // 或 'opencode'
});
```

## 测试验证

1. 配置为 `claude` 时，调用 `executeTaskWithClaude`，使用 `claude` 命令执行
2. 配置为 `opencode` 时，调用 `executeTaskWithOpenCode`，使用 `opencode` 命令执行
3. 日志正确记录 `executor` 字段

## 后续任务

- R54.9.5: 添加配置相关的消息处理，使 webview 和扩展端同步配置
- R54.9.6: 添加单元测试验证配置功能

## 审查状态

待审查
