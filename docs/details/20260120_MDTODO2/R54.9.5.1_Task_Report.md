# R54.9.5.1 任务报告

## 任务描述

在 `webviewProvider.ts` 中添加 `executionModeChanged` 消息处理，接收 webview 发送的执行模式变更消息并保存到 `settings.json`。

## 修改内容

### 1. 导入 SettingsService (`src/providers/webviewProvider.ts`)

```typescript
import { SettingsService } from '../services/settingsService';
```

### 2. 添加 SettingsService 实例属性

```typescript
// R54.9.5: 设置服务实例
private settingsService!: SettingsService;
```

### 3. 在构造函数中初始化 SettingsService

```typescript
// R54.9.5: 初始化设置服务
this.settingsService = new SettingsService();
```

### 4. 添加 executionModeChanged 消息处理 case

```typescript
// R54.9.5: 处理执行模式变更消息
case 'executionModeChanged':
  await this.handleExecutionModeChanged(message.mode);
  break;
```

### 5. 添加 handleExecutionModeChanged 方法

```typescript
// R54.9.5: 处理执行模式变更
private async handleExecutionModeChanged(mode: 'claude' | 'opencode'): Promise<void> {
  const workspacePath = this.getWorkspaceFolderPath();
  if (!workspacePath) {
    console.error('[MDTODO] No workspace path found for saving execution mode');
    return;
  }

  try {
    await this.settingsService.updateExecutionMode(workspacePath, mode);
    console.log(`[MDTODO] Execution mode saved: ${mode}`);

    // 发送确认消息给 webview
    this.sendToWebview({
      type: 'executionModeUpdated',
      mode: mode
    });
  } catch (error) {
    console.error('[MDTODO] Failed to save execution mode:', error);
  }
}
```

### 6. 修改 sendToWebview 方法，初始加载时发送 executionMode

```typescript
private sendToWebview(customMessage?: any): void {
  const panel = this.panelManager.getPanel();
  if (panel) {
    if (customMessage) {
      panel.webview.postMessage(customMessage);
    } else {
      const workspacePath = this.getWorkspaceFolderPath();
      // R54.9.5: 获取当前执行模式并发送给 webview
      this.settingsService.readSettings(workspacePath).then(config => {
        panel.webview.postMessage({
          type: 'updateTasks',
          tasks: this.serializeTasks(this.currentTasks),
          textBlocks: this.currentTextBlocks,
          filePath: this.currentFilePath,
          workspacePath: workspacePath,
          executionMode: config.executionMode
        });
      }).catch(error => {
        console.error('[MDTODO] Failed to read execution mode:', error);
        panel.webview.postMessage({
          type: 'updateTasks',
          tasks: this.serializeTasks(this.currentTasks),
          textBlocks: this.currentTextBlocks,
          filePath: this.currentFilePath,
          workspacePath: workspacePath,
          executionMode: 'claude' // 默认值
        });
      });
    }
  }
}
```

## 功能说明

1. **消息接收**: 当用户通过 webview UI 更改执行模式时，webview 会发送 `executionModeChanged` 消息到扩展端
2. **保存设置**: 扩展端调用 `settingsService.updateExecutionMode` 将设置保存到 `.vscode/mdtodo/settings.json`
3. **同步确认**: 保存成功后发送 `executionModeUpdated` 消息回 webview 确认
4. **初始加载**: 每次 `updateTasks` 消息发送时，包含当前 `executionMode` 值

## 编译验证

```
webpack 5.104.1 compiled successfully in 3373 ms (extension.js)
webpack 5.104.1 compiled successfully in 4769 ms (bundle.js)
```

## 依赖关系

- `src/services/settingsService.ts` - 提供 `updateExecutionMode` 和 `readSettings` 方法
- `src/services/claudeService.ts` - 执行任务时从 settingsService 读取配置

## 消息类型

| 方向 | 类型 | 数据 |
|------|------|------|
| webview -> extension | `executionModeChanged` | `{ type: 'executionModeChanged', mode: 'claude' \| 'opencode' }` |
| extension -> webview | `executionModeUpdated` | `{ type: 'executionModeUpdated', mode: 'claude' \| 'opencode' }` |
| extension -> webview | `updateTasks` (新增) | 包含 `executionMode` 字段 |

## 完成状态

[COMPLETED]
