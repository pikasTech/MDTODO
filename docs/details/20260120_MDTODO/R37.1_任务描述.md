## 任务描述

这个打开md原文用vscode打开（注意其他的还是用typero不变）

## 执行记录

### 修改内容

#### 1. webviewProvider.ts

修改 `handleOpenSourceFile` 方法，将 Typora 打开改为 VSCode 打开：

```typescript
/**
 * 【实现R37】【修改R37.1】打开原MDTODO文件（使用 VSCode）
 * 说明：md原文用VSCode打开，其他链接（如details里的文件）仍用Typora打开
 */
private async handleOpenSourceFile(): Promise<void> {
  if (!this.currentFilePath) {
    vscode.window.showWarningMessage('未加载任何文件');
    return;
  }

  // 检查文件是否存在
  if (!fs.existsSync(this.currentFilePath)) {
    vscode.window.showErrorMessage(`文件不存在: ${this.currentFilePath}`);
    return;
  }

  console.log('[MDTODO] 打开原文件:', this.currentFilePath);

  // 【R37.1】使用 VSCode 打开 md 原文
  const uri = vscode.Uri.file(this.currentFilePath);
  await vscode.window.showTextDocument(uri);
}
```

**修改说明**：
- 移除了对 `openWithTypora` 的调用
- 改为直接使用 `vscode.window.showTextDocument(uri)` 在 VSCode 编辑器中打开文件

#### 2. TaskList.tsx

更新按钮的 title 属性：

```typescript
React.createElement('button', {
  className: `btn btn-secondary ${buttonCooldown ? 'disabled' : ''}`,
  disabled: buttonCooldown,
  onClick: handleOpenSourceFile,
  title: '在VSCode中打开原文'  // 从 '在Typora中打开原文' 改为此
}, '打开原文'),
```

### 功能说明

1. **打开原文点击后在 VSCode 编辑器中打开当前 MDTODO 文件
2. **任务内链接**：点击按钮**：任务内的文档链接（如 details 里的文件）仍然使用 Typora 打开
3. **分工明确**：
   - "打开原文"按钮 → VSCode 编辑器打开
   - 任务内链接 → Typora 打开

### 测试方法

1. 在 VSCode 中打开一个 MDTODO 文件（如 20260120_MDTODO.md）
2. 点击 Webview 界面右上角的"打开原文"按钮
3. 验证文件是否在 VSCode 新标签页中打开
4. 点击任务内的 details 链接，验证是否仍在 Typora 中打开

### 完成状态

✅ 已完成
